#ifndef REI_H
#define REI_H

class Rei {
public:
    Rei();
    // Outros métodos e atributos do rei
    int posicaox; // Atributo para armazenar a posição x do bispo
    int posicaoy; // Atributo para armazenar a posição y do bispo
    bool estavivo();
};

#endif // REI_H